{
  "name": "jogeum-ledger",
  "version": "1.0.0",
  "description": "조금만 가계부 — 무빌드 PWA. 이 package.json은 테스트 실행용이며 앱 자체는 의존성이 없습니다.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node tests/run.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
